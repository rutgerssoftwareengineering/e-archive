To run unit tests:

Start in Minerva-Web-App directory in the /1_code directory.
Run the command 'npm jest' to see unit tests


There is no unit testing for mobile as it is not yet connected to backend

Do not run unit tests from this folder, this folder only holds the files for easier viewing
